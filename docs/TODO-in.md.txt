Посмотри текущие настройки gnome - В текущих настройках gnome у меня в верхней полоски три кнопки:
- овал - показывает список работающих приложений плиткой
- apps - меню приложений - как windows start menu
- places - меню папок
примерно такие же иконки я хочу иметь возможность настроить в плагине 

необходимо реализовать:
- Настройки не применяются сразу - надо перезапускать плагин
- самая левая иконка - иконка для перетаскивания виджета. Клик на неё не дошлжен открывать Activities
- из popup меню по правой кнопке на иконку перетаскивания надо убрать разделитель Widgets
- правая кнопка мыши на иконке перетаскивания почему-то срабатывает через раз
- в настройках виджетов меню/activities/places должно быть видно иконку, а не мнемоническое название иконки и должна быть возможность выбирать из иконок - с поиском
- в сабвиджете меню должно показываться меню - такое же, как показывается в xfce, аналог старого меню start в windows.  
- в настройках не надо разделять Widgets + Panel - все настройки на одном экране
- в настройках надо добавить иконку плюс (+) для добавления новых элементов с меню, а не рисовать все неустановленные виджеты
- настройка position не работает - я поменял, перезапускал, виджет не перемещался
- надо в position добавить пункт "плавающий индикатор" - чтобы виджет никуда не прилипал, сохранял местоположение там, куда я его поместил
- настройка vertical orientation не работает - либо убери её и добавь в todo, либо исправь

по ходу выполнения обновляй документацию, обновляй index

## Статус реализации (все пункты выполнены)

- [x] Настройки применяются на лету — панель следит за `widgets.json`
  (`Gio.FileMonitor`) и пересоздаёт виджеты без перезапуска Shell.
- [x] Левый клик по иконке перетаскивания больше не открывает Activities (no-op).
- [x] Из popup-меню убран разделитель «Widgets» (осталось только «Settings…»).
- [x] Правый клик по иконке перетаскивания срабатывает надёжно (гонка с
  `ClickGesture` устранена — состояние меню снимается на press).
- [x] В настройках меню/activities/places — превью иконки и выбор из иконок с
  поиском (`plugins/iconPicker.ts`).
- [x] Сабвиджет `gnome-menu` показывает меню приложений по категориям
  (аналог XFCE / старого Start).
- [x] Все настройки на одном экране (Widgets + Panel объединены).
- [x] Добавление виджетов — кнопка «+» в шапке группы (не список всех).
- [x] Позиция применяется при загрузке (стартовый `_relocate`, отложен до
  `mapped`, чтобы не спамить St-варнингами).
- [x] Пресет позиции «Floating (keep position)» — виджет остаётся там, куда его
  поместили.
- [x] Vertical orientation — работает (симптом был вызван багом позиции).

Верификация: `npm run typecheck`, `npm test` (19/19), headless-загрузка
(`EXT_STATE=1`, 0 варнингов «not in the stage`), харнесс всех страниц настроек
(`failures=0`). Живьём в GUI не проверялось.
